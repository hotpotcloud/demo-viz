export const viewerOptions = {
    camera: {
      position: [2000, 2000, 2000], // 相机位置
      lookAt: [0, 0, 0], // 相机聚焦目标
    },
    scale: {
      vertical: 2, // 垂直方向缩放
    },
    lighting: {
      ambient: 0.5, // 环境光照
      directional: { x: 1, y: 1, z: 1 }, // 方向光照
  },
    
  };